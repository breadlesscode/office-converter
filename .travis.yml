language: php
php:
  - '7.1'
  - '7.2'

before_install:
  - sudo apt-get update -qq

install:
  - sudo apt-get install -y libreoffice
  - composer install

script:
  - libreoffice --version
  - composer test
